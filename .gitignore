__pycache__/
*.pyc
.venv/
/scratchpad/

# Local plugin install / run state (regenerated by Claude Code)
/.claude/

# Non-plugin leftovers from the full JANUS repo (live on the full branch, not in the slim plugin)
/cases/
/labs/
/intake/
/docs/
/tools/
/review-queue/
/schema/
/teams/

# Crash dumps / debug binaries — huge and may contain customer data. Never commit.
*.vmcore
*.kdump
vmcore
vmlinux

# Red Hat-internal/licensed brand template — not ours to redistribute publicly.
# The deck skill works with any user-supplied .pptx template; this default is
# local-only. See plugins/janus/skills/deck/SKILL.md.
/plugins/janus/skills/deck/template/
